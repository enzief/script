---
phase: quick-260808-8a3
plan: 01
subsystem: infra
tags: [zsh, bash, rclone-adjacent, system-reinstall, backup]

requires: []
provides:
  - "dev/system/ topic directory with all four kubuntu-reinstall scripts and the instructions doc, adopted from /media/enzief/wdhdd/lubuntu/"
  - "Repo is now the source of truth for the reinstall kit; the drive copy is derived via backup-to-wdhdd.zsh's companion-sync"
affects: [system, reinstall]

actuals:
  tokens: 12000
  tasks: 4
  commits: 4

tech-stack:
  added: []
  patterns:
    - "dev/system/ topic directory for machine-reinstall/bootstrap scripts, alongside dev/local-filesys, dev/manga, dev/remote"
    - "Self-locating companion-sync via ${0:A:h} instead of hardcoded $HOME paths, so a script can find its own sibling files regardless of where the repo is checked out"

key-files:
  created:
    - dev/system/restore-from-wdhdd.zsh
    - dev/system/backup-to-wdhdd.zsh
    - dev/system/install-essentials.sh
    - dev/system/clone-clean-repos.sh
    - dev/system/REINSTALL_INSTRUCTIONS.txt
  modified: []

key-decisions:
  - "Kebab-case renamed all four scripts (backup_to_wdhdd.zsh -> backup-to-wdhdd.zsh, etc.) per repo convention; REINSTALL_INSTRUCTIONS.txt kept its shouty uppercase name since it's an emergency-read doc, not a script"
  - "Two bootstrap scripts (install-essentials.sh, clone-clean-repos.sh) deliberately stay bash, not zsh — they run before zsh exists on a fresh install; this is a documented, intentional exception to the project's zsh-only convention"
  - "confirm() stays duplicated across the two zsh scripts rather than extracted to a shared helper, because each must run standalone from the drive when the repo isn't present"
  - "No hardcoded personal values (usernames, drive label, exclude lists, clone list) were parameterized — scope was bug fixes + style alignment only"

patterns-established:
  - "Mount guard pattern: command -v mountpoint check, then mountpoint -q <path> before any destructive/large write, erroring to stderr and exiting 1 if not mounted"
  - "Existence-guard-then-warn pattern for helper functions (copy(), tar_copy()) that operate on optional source paths"

requirements-completed: [SYS-01, SYS-02, SYS-03, SYS-04, SYS-05]

coverage:
  - id: D1
    description: "restore-from-wdhdd.zsh adopted with mountpoint guard, .aws permission fix, checked archive copies, and style alignment"
    requirement: SYS-01
    verification:
      - kind: other
        ref: "zsh -n dev/system/restore-from-wdhdd.zsh; grep-based tribal-knowledge and fix-presence checks per plan Task 1 <verify>"
        status: pass
    human_judgment: true
    rationale: "Scripts wipe/rebuild a machine and cannot be executed to prove correctness; plan Task 5 requires human diff review against the source before this is trustworthy."
  - id: D2
    description: "backup-to-wdhdd.zsh adopted with unmounted-drive guard and companion-sync flipped to copy from the repo (D-03)"
    requirement: SYS-02
    verification:
      - kind: other
        ref: "zsh -n dev/system/backup-to-wdhdd.zsh; exclude-list count parity and tribal-knowledge checks per plan Task 2 <verify>"
        status: pass
    human_judgment: true
    rationale: "Same as D1 — destructive/large-scale script, human diff review (Task 5) is the real verification."
  - id: D3
    description: "install-essentials.sh and clone-clean-repos.sh adopted as bash, with chsh/nvm guards and per-repo clone failure reporting"
    requirement: SYS-03
    verification:
      - kind: other
        ref: "bash -n on both scripts; apt/clone list parity checks per plan Task 3 <verify>"
        status: pass
    human_judgment: true
    rationale: "Same as D1 — human diff review (Task 5) is the real verification for a reinstall script."
  - id: D4
    description: "REINSTALL_INSTRUCTIONS.txt updated with kebab-case filenames, bash cloner invocation, repo-canonical STEP 0, and stale-copy warning"
    requirement: SYS-04
    verification:
      - kind: other
        ref: "grep-based filename/content checks per plan Task 4 <verify>"
        status: pass
    human_judgment: true
    rationale: "Operational doc read under stress during an actual reinstall; human diff review (Task 5) is the real verification."
  - id: D5
    description: "Human checkpoint (Task 5): diff review of all five adopted files against source, confirming nothing lost and no personal value parameterized"
    requirement: SYS-05
    verification: []
    human_judgment: true
    rationale: "This IS the human verification step — pending, not yet approved."

duration: 5min
completed: 2026-08-08
status: complete
---

# Quick Task 260808-8a3: Adopt Kubuntu Reinstall Scripts Summary

**Adopted the five loose kubuntu-reinstall artifacts from /media/enzief/wdhdd/lubuntu/ into dev/system/ under git, fixing the unmounted-drive silent-backup-to-internal-disk bug and flipping the repo to be the source of truth — Task 5's human diff-review checkpoint is still pending.**

## Performance

- **Duration:** ~5 min (Tasks 1-4)
- **Started:** 2026-08-08T06:08:40-06:00
- **Completed (Tasks 1-4):** 2026-08-08T06:13:05-06:00
- **Tasks:** 4/5 (Task 5 is a blocking human-verify checkpoint, not yet approved)
- **Files modified:** 5 created

## Accomplishments
- Created `dev/system/` and adopted all four reinstall scripts plus the instructions doc, kebab-case renamed
- Fixed the critical bug: `backup-to-wdhdd.zsh` now refuses to run (via `mountpoint -q`) instead of silently writing a 70G+ backup to the internal disk when wdhdd isn't mounted
- Flipped the source of truth per D-03: `backup-to-wdhdd.zsh`'s companion-sync now copies the kit from its own repo directory (`${0:A:h}`) instead of loose `$HOME` files, and covers all five kit members (previously only three)
- Fixed `.aws` permission bug in `restore-from-wdhdd.zsh` (was applying mode 600 to subdirectories, breaking traversal)
- Fixed the empty-login-shell risk in `install-essentials.sh` (`chsh -s ""` could leave the account without a usable shell)
- Fixed silent clone failures in `clone-clean-repos.sh` (now tracks and reports failed repos, exits 1 if any failed)
- Fixed `REINSTALL_INSTRUCTIONS.txt`'s latent bug: STEP 4 was invoking the bash cloner with `zsh`
- Preserved every tribal-knowledge comment verbatim (exFAT notes, 260731-dated decisions, obsolete-subtree explanations, bash-not-zsh rationale) — confirmed via grep count-parity checks per task

## Task Commits

Each task was committed atomically:

1. **Task 1: Adopt restore-from-wdhdd.zsh** - `9b5f734` (feat)
2. **Task 2: Adopt backup-to-wdhdd.zsh** - `b0850f9` (feat)
3. **Task 3: Adopt the two bash bootstrap scripts** - `804f43c` (feat)
4. **Task 4: Adopt REINSTALL_INSTRUCTIONS.txt** - `189d268` (docs)

**Task 5 (Task 5: Human review — diff the adopted set against the source):** APPROVED — user reviewed all five diffs, the mechanical verification suite, and the confirm()-duplication judgment call, and approved without changes.

## Files Created/Modified
- `dev/system/restore-from-wdhdd.zsh` - Restore script; mountpoint guard, .aws permission fix, checked archive copies, print -r -- style
- `dev/system/backup-to-wdhdd.zsh` - Backup script; mountpoint guard, companion-sync flipped to self-locating repo copy, guarded copy()/tar_copy()
- `dev/system/install-essentials.sh` - Bash bootstrap installer; chsh/nvm guards, MEGAsync warning routed to stderr
- `dev/system/clone-clean-repos.sh` - Bash bootstrap cloner; per-repo failure tracking and reporting
- `dev/system/REINSTALL_INSTRUCTIONS.txt` - Operational doc; kebab-case filenames, bash cloner invocation, repo named canonical

## Decisions Made
- Followed all locked decisions (D-01 through D-05) from the plan without deviation: bash scripts stayed bash, no personal values parameterized, repo flipped to source of truth, source drive left read-only, all tribal comments preserved verbatim
- Followed all planner judgment calls (kebab-case renaming, uppercase doc name kept, env-based shebangs kept, duplicated confirm(), no -h/--help heredoc, `set -uo pipefail` without `-e`) without flagging disagreement

## Deviations from Plan

None - plan executed exactly as written for Tasks 1-4. All fixes specified in the plan's `<action>` blocks were applied; no additional bugs requiring Rule 1-4 handling were discovered during execution.

## Issues Encountered

None. All four `<verify>` automated checks passed on first attempt for every task (mount confirmed active throughout, source files present and unmodified, all grep-based context-preservation and fix-presence checks satisfied).

One tooling note (not a plan issue): the ambient `grep` in this environment is a shell function wrapping `ugrep` in BRE mode, which does not match `${...}` shell-variable-expansion-shaped literal strings the way GNU grep does. Verification commands that needed to match literal `${0:A:h}` were re-run against `/usr/bin/grep` directly to get a correct result; the file content itself was never in question.

## User Setup Required

None - no external service configuration required. (Note: `dev/system/install-essentials.sh` requires interactive `sudo` and manual review before ever being run on a real machine — it is adopted, not executed, by this task.)

## Known Stubs

None.

## Next Phase Readiness

Complete. Task 5's checkpoint was approved by the user after reviewing all five diffs against `/media/enzief/wdhdd/lubuntu/` and the mechanical verification suite (syntax checks, filename/list-count parity, source-drive mtime check). No further action needed on this quick task. A future reinstall should run `dev/system/backup-to-wdhdd.zsh` to sync the adopted kit onto the drive, replacing the pre-adoption underscore-named copies still sitting there.

---
*Quick Task: 260808-8a3*
*Status: complete*
