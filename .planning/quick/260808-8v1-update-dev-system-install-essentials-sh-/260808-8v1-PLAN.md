---
phase: quick-260808-8v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/system/backup-to-wdhdd.zsh
  - dev/system/install-essentials.sh
autonomous: false
requirements: [SYS-06, SYS-07, SYS-08]

estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The nvm Tier-2 prompt in backup-to-wdhdd.zsh points at a path that actually exists, so answering yes produces a real backup instead of a silent no-op (D-08)"
    - "No Tier-2 prompt asks about an application that is not installed and has no config directory on this machine (D-09)"
    - "A fresh Kubuntu restore recovers panel/widget layout, global shortcuts, window-manager rules, multi-monitor layout, keyboard-layout switching, default app associations, and per-app settings for Dolphin/Konsole/Kate/Gwenview/Okular/Spectacle without manual reconfiguration (D-07)"
    - "No credential-bearing or clipboard-history directory is newly copied onto the unencrypted exFAT drive"
    - "install-essentials.sh's apt package lists are byte-identical to their pre-plan state, signal-desktop and telegram-desktop included (D-06)"
    - "Every pre-existing tribal-knowledge comment in both touched files survives verbatim; the only removal is the deliberate D-09 block"
    - "restore-from-wdhdd.zsh and REINSTALL_INSTRUCTIONS.txt require no edit, and are confirmed unmodified"
  artifacts:
    - dev/system/backup-to-wdhdd.zsh
    - dev/system/install-essentials.sh
  key_links:
    - "backup-to-wdhdd.zsh copy_file '.config/$f' -> $DEST/dotfiles/.config/$f -> restore-from-wdhdd.zsh's blanket rsync of $SRC/dotfiles/ into $HOME -> $HOME/.config/$f (no restore-side change needed)"
    - "The corrected .nvm Tier-2 entry -> dotfiles/.nvm/ -> restore -> $HOME/.nvm, which is exactly the NVM_DIR install-essentials.sh sets"
    - "The extended .local/share loop -> dotfiles/.local/share/{konsole,kate} -> Konsole saved profiles and Kate session state on the new machine"
---

<objective>
Bring `dev/system/backup-to-wdhdd.zsh` in line with this machine's actual post-migration Kubuntu/Plasma state: fix a dead Tier-2 path, drop a Tier-2 entry for software that is not installed, and add curated KDE Plasma desktop-config coverage that never existed under the old Lubuntu/LXQt setup. Separately, confirm (rather than assume) that `install-essentials.sh` needs no package-list changes.

Purpose: The prior task (260808-8a3) adopted these scripts as-is from the external drive — they still describe the pre-migration Lubuntu machine. Right now a real reinstall would restore shell dotfiles and app data perfectly and then drop you into a stock Plasma desktop with no panel layout, no keyboard shortcuts, no window rules, and no default app associations — hours of tedious manual reconstruction that a ~40KB list of rc files prevents.

Output: An updated `backup-to-wdhdd.zsh` whose Tier-1 and Tier-2 lists match reality, and a verified-accurate `install-essentials.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

Files under edit:
@dev/system/backup-to-wdhdd.zsh
@dev/system/install-essentials.sh

Read-only context (confirm no change needed; do NOT edit):
@dev/system/restore-from-wdhdd.zsh
@dev/system/REINSTALL_INSTRUCTIONS.txt

Established fix/style patterns from the adoption task:
@.planning/quick/260808-8a3-adopt-kubuntu-reinstall-scripts-from-med/260808-8a3-PLAN.md
</context>

<locked_decisions>
Settled with the user before planning. Do NOT revisit or "improve" past them.

- **D-06 — signal-desktop and telegram-desktop stay in install-essentials.sh's apt list**, even though neither is installed here (verified absent from `dpkg -l`, `snap list`, `flatpak list`). This script describes the target state for a fresh machine, not an inventory of this one. No change.
- **D-07 — KDE Plasma config coverage is curated, not comprehensive.** Hand-pick the files/dirs holding genuine user customization, matching the script's existing per-app-named style. A blanket or exclude-based copy of all of `~/.config` was explicitly rejected.
- **D-08 — Fix the nvm Tier-2 path.** nvm installs to `~/.nvm`, never under `~/.config`, so the existing entry has always been dead code (its `[ -e "$HOME/$src" ]` guard never fires). Point it at the real path. Keep the "(recommend: No)" wording; correct the size annotation.
- **D-09 — Drop the VS Code Tier-2 confirm block entirely.** VS Code is not installed (no config dir, no binary on PATH, verified 260808). Deliberate removal, explicitly chosen over keeping a dead entry — this is the one sanctioned exception to D-05's preserve-everything rule.

Still in force from the prior task:

- **D-02 — No parameterization.** Hardcoded personal values (usernames, drive label, paths, exclude lists) stay hardcoded. No new flags, no config file.
- **D-04 — `/media/enzief/wdhdd/lubuntu/` is out of scope entirely.** Do not read from it, write to it, or diff against it in this task. Only the repo copies under `dev/system/` are touched.
- **D-05 — Preserve tribal knowledge verbatim.** Existing comments are copied through unchanged. Additions are fine; deletions and rewordings are not, except the D-09 block.
</locked_decisions>

<planner_decisions>
Judgment calls made during planning, each spot-checked against this machine on 260808. Follow them; push back at the checkpoint if you disagree.

**Curation principle used:** include a file if losing it means manually redoing a setting the user deliberately chose; skip it if it is derived, ephemeral, redownloadable, or a default that was never touched. This is the boundary that makes the list defensible rather than "everything small."

**Included — 18 loose files directly under `~/.config`** (all verified present as regular files, ~35KB total, so Tier 1 without hesitation):

- Plasma/global: `kdeglobals` (theme/fonts/colors), `kglobalshortcutsrc` (15K of global keyboard shortcuts — the single largest reconstruction cost in the set), `kwinrc` (window manager rules), `kwinoutputconfig.json` (multi-monitor layout), `plasma-org.kde.plasma.desktop-appletsrc` (panel/widget layout), `plasmashellrc` (panel/shell settings — its companion), `kxkbrc` (keyboard-layout switching, directly relevant given fcitx5/Vietnamese input is already first-class here)
- Associations: `mimeapps.list` (default app bindings — easy to lose, tedious to redo)
- Per-app, all for apps already in the script's orbit: `dolphinrc`, `konsolerc`, `konsolesshconfig`, `katerc`, `kate-externaltoolspluginrc`, `katevirc`, `gwenviewrc`, `okularrc`, `okularpartrc`, `spectaclerc`

`plasmashellrc` was not on the user's candidate list but was verified present (1813 bytes) and is the direct companion to the appletsrc panel layout; the user's brief explicitly invited judgment on the final list.

**Included — 2 additions to the existing `.local/share` loop:** `konsole` (8K, saved terminal profiles — real customization) and `kate` (16K, editor session/state, directly analogous to the `dolphin` and `kxmlgui5` entries already in that loop).

**Deliberately skipped, each with a one-line comment in the script** (the file already establishes this pattern with its `.bmad is pure cache` note):

- `.local/share/klipper` (588K sqlite clipboard history) — user data, not config, and clipboard history routinely contains pasted passwords and tokens. Copying it onto an unencrypted exFAT drive is a real disclosure risk for zero configuration value.
- `.config/kdeconnect` — holds `privateKey.pem` and `certificate.pem` (mode 600). exFAT stores no permissions and `restore-from-wdhdd.zsh`'s permission-fix pass covers only `.ssh`, `.gnupg`, and `.aws` — so this key would land group/world-readable on the new machine. Re-pairing a phone is a 30-second operation. Not worth the exposure.
- `.local/share/claude` (844M, contains only `versions/`) — Claude Code's downloaded release binaries, fully redownloadable. `$HOME/.claude` (the real config and project state) is already backed up on line 103 and is untouched.
- `.local/share/ark` — recent-archive list only, no customization.
- `.config/kdedefaults` — derived state regenerated whenever a Global Theme is applied; `kdeglobals` already carries the effective result.
- The remaining ~70 `~/.config` entries — app-generated caches, session state, or untouched defaults. Verified non-candidates worth noting: `.config/rclone` is an empty directory with no `rclone.conf`, `.config/uv` holds only install metadata, `.config/git` holds only a gitk view-state file (real git config is `~/.gitconfig`, already covered on line 96), and `.config/fish` is essentially empty while zsh is the actual login shell.

**Call shape matters:** loose files under `~/.config` go through `copy_file`, not the existing directory loop's `copy`. `copy` appends a trailing slash and is for directories; `copy_file`'s `[ -f ... ]` guard and silent skip are exactly right here, since a partially-configured fresh KDE install legitimately won't have every one of these. Do not try to force loose files through the directory-loop pattern.

**`restore-from-wdhdd.zsh` needs no change and must not be edited.** Its dotfiles restore is a single blanket `rsync "$SRC/dotfiles/" "$HOME/"`, so every entry added here rides along automatically. It contains no nvm reference and no VS Code reference, so neither D-08 nor D-09 has a restore-side mirror. Its permission-fix pass stays limited to `.ssh`/`.gnupg`/`.aws` — which is precisely why kdeconnect is skipped rather than added.

**`REINSTALL_INSTRUCTIONS.txt` needs no change and must not be edited.** It names the five kit filenames (unchanged) and summarizes the installed package set (unchanged per D-06). Nothing it documents moves.

**User-data sections are untouchable.** The `work/`, `Documents/`, `Pictures/`, `Apps/`, `Downloads/`, `Videos/`, `renikov/`, `_recover/`, and loose-files blocks, and every `--exclude` in them, are out of scope for this task. Do not reflow, reorder, or "tidy" them.
</planner_decisions>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Correct the two stale Tier-2 entries (D-08, D-09)</name>
  <files>dev/system/backup-to-wdhdd.zsh</files>
  <read_first>
    /home/enzief/work/iswi/script/dev/system/backup-to-wdhdd.zsh — the Tier 2 section only (from the `confirm()` definition to the companion-sync banner).
  </read_first>
  <action>
This is the tracer: two surgical edits inside one section that exercise the whole discipline this plan needs — an in-place correction, a deliberate deletion, and proof that nothing around them moved. Get this right before Task 2 adds anything.

<!-- planner-discipline-allow: .config/nvm -->

**D-08 — repair the dead nvm entry.** The Tier 2 block whose prompt begins with `.config/nvm` is unreachable: nvm installs into `$HOME/.nvm` and has never created a directory under `~/.config`, so `copy()`'s existence guard has silently warned-and-returned on every run this script has ever had. Point both the prompt text and the `copy` call at `.nvm`, with the destination `dotfiles/.nvm/` so it restores to `$HOME/.nvm` — which is exactly the `NVM_DIR` that `install-essentials.sh` sets.

Correct the size annotation while you are there: the directory measures 236M on this machine as of 260808, not the 1.1G the prompt currently claims. Keep the `redownloadable node versions` phrasing and keep the `[recommend: No]` suffix verbatim — the recommendation is if anything stronger now, since `install-essentials.sh` reinstalls nvm and runs `nvm install --lts` before the restore ever runs.

Add one short comment above the block recording why the path changed: that the original entry named a path nvm never creates, so the prompt copied nothing for its entire life, and that the size was re-measured on 260808. This is exactly the kind of note D-05 exists to protect, so write it as tribal knowledge for the next reader.

**D-09 — delete the VS Code entry.** Remove the entire Tier 2 `if confirm ... fi` block whose prompt names VS Code's config directory (the one carrying the 1.4G size annotation and a run of cache excludes), including its `copy` call and its exclude list. VS Code is not installed on this machine: no config directory, no binary on PATH, verified 260808. Delete it outright — no replacement comment, no commented-out remnant. This is the single sanctioned deletion in this plan; it does not license removing anything else.

Change nothing else in the Tier 2 section. The Downloads, Videos, TelegramDesktop, signal-cli, BraveSoftware, Firefox, and hakuneko-desktop blocks keep their exact prompts, size annotations, excludes, and order. The four warlord `tar_copy` lines and their explanatory comment stay untouched.
  </action>
  <verify>
    <automated>F=/home/enzief/work/iswi/script/dev/system/backup-to-wdhdd.zsh; zsh -n "$F" || exit 1; n=$(grep -v '^[[:space:]]*#' "$F" | grep -c 'workspaceStorage' || true); [ "$n" -eq 0 ] || { echo "FAIL: VS Code block still present"; exit 1; }; n=$(grep -v '^[[:space:]]*#' "$F" | grep -c 'config/nvm' || true); [ "$n" -eq 0 ] || { echo "FAIL: stale nvm path still live"; exit 1; }; grep -q 'copy "\.nvm/" "dotfiles/\.nvm/"' "$F" || { echo "FAIL: nvm copy not repointed"; exit 1; }; grep -q 'recommend: No' "$F" || { echo "FAIL: recommendation wording lost"; exit 1; }; c=$(grep -c 'if confirm ' "$F" || true); [ "$c" -eq 8 ] || { echo "FAIL: expected 8 confirm blocks, found $c"; exit 1; }; for t in 260731 exFAT symlinks obsolete nubank hakuneko Crashpad; do grep -q -- "$t" "$F" || { echo "FAIL: lost context marker $t"; exit 1; }; done; echo PASS</automated>
  </verify>
  <done>`backup-to-wdhdd.zsh` parses under `zsh -n`; exactly 8 Tier-2 confirm blocks remain (was 9); the nvm block copies `.nvm/` to `dotfiles/.nvm/` and keeps its recommendation wording; no live reference to the old nvm path or to the VS Code block survives; every pre-existing tribal-knowledge marker is still present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add curated KDE Plasma config coverage to Tier 1 (D-07)</name>
  <files>dev/system/backup-to-wdhdd.zsh</files>
  <read_first>
    /home/enzief/work/iswi/script/dev/system/backup-to-wdhdd.zsh — the Tier 1 section, specifically the `copy_file` helper definition, the existing `.config/$f` directory loop, the `.local/bin/$f` loop, and the `.local/share/$f` loop.
  </read_first>
  <action>
<!-- planner-discipline-allow: klipper -->
<!-- planner-discipline-allow: kdeconnect -->

Add curated Plasma desktop-config coverage to Tier 1. These are the settings that survive nothing today: a restore currently rebuilds shell dotfiles and per-app data perfectly and then hands you a stock Plasma desktop.

**New loose-file loop.** Immediately after the existing `for f in shabontama go fcitx5 ...` directory loop (and before the Signal block), add a new loop that iterates the 18 filenames below and calls `copy_file` with source `".config/$f"` and destination `"dotfiles/.config/$f"`. Use `copy_file`, not `copy` — these are regular files, and `copy_file`'s `[ -f ... ]` guard and silent skip are the documented right behavior for entries a partially-configured fresh KDE install may legitimately lack. Do not add trailing slashes.

Emit the names in three visually grouped runs, one group per line continuation, so the list stays readable — Plasma/global first, then the associations file, then per-app:

- Plasma/global: `kdeglobals`, `kglobalshortcutsrc`, `kwinrc`, `kwinoutputconfig.json`, `plasma-org.kde.plasma.desktop-appletsrc`, `plasmashellrc`, `kxkbrc`
- Associations: `mimeapps.list`
- Per-app: `dolphinrc`, `konsolerc`, `konsolesshconfig`, `katerc`, `kate-externaltoolspluginrc`, `katevirc`, `gwenviewrc`, `okularrc`, `okularpartrc`, `spectaclerc`

Precede the loop with a short comment block explaining what it is and why it is curated rather than blanket: these are the loose KDE rc files sitting directly under `~/.config` (the directory loop above only reaches subdirectories, so nothing here overlaps it — note specifically that `.config/kate/` the directory is already covered above while `katerc` and friends are separate loose files alongside it), the whole set is roughly 35KB, and the selection principle is "a setting the user deliberately chose, versus derived or default state." Mention that this section is new post-migration — none of it existed under the old LXQt setup — so a future reader understands why it appeared without a corresponding bug.

**Extend the `.local/share` loop.** Append `konsole` and `kate` to the existing `for f in kwalletd dolphin gwenview okular kxmlgui5` list, in that order, keeping the five existing entries first and unchanged. `konsole` holds saved terminal profiles, `kate` holds editor session state; both are small (8K and 16K) and both are real customization, directly analogous to the `dolphin` and `kxmlgui5` entries already there. Add a brief trailing comment naming what the two new ones hold.

**Record the deliberate skips.** Add one short comment block near the new loops listing what was considered and rejected, following the precedent already set by the `.bmad is pure cache -- intentionally skipped entirely` line. Cover, one line each: the clipboard-history store under `.local/share` (user data rather than config, and it routinely holds pasted secrets that should not land on an unencrypted exFAT drive); the KDE phone-pairing config directory (contains an unencrypted device private key, and the restore script's permission-fix pass covers only `.ssh`/`.gnupg`/`.aws`, so it would be restored world-readable — re-pairing takes seconds); `.local/share/claude` (844M of redownloadable Claude Code release binaries; `$HOME/.claude`, the real config, is already covered above); and a single closing line noting the remaining `~/.config` entries are app-generated caches, session state, or untouched defaults and were deliberately not curated. Keep this block tight — it is a signpost, not an inventory.

Touch nothing outside these three additions. In particular, do not modify the `.ssh`/`.gnupg`/`.aws` loop, the top-level dotfiles loop, the `.claude`/`.gsd`/`.codex`/`.codemoss` copies, the Signal block, the `.local/bin` loop, or anything from the `work/` block onward — all of the user-data sections are explicitly out of scope for this task.
  </action>
  <verify>
    <automated>F=/home/enzief/work/iswi/script/dev/system/backup-to-wdhdd.zsh; zsh -n "$F" || exit 1; for t in kdeglobals kglobalshortcutsrc kwinrc kwinoutputconfig.json plasma-org.kde.plasma.desktop-appletsrc plasmashellrc kxkbrc mimeapps.list dolphinrc konsolerc konsolesshconfig katerc kate-externaltoolspluginrc katevirc gwenviewrc okularrc okularpartrc spectaclerc; do grep -q -- "$t" "$F" || { echo "FAIL: missing curated entry $t"; exit 1; }; done; grep -q 'copy_file ".config/\$f" "dotfiles/.config/\$f"' "$F" || { echo "FAIL: loose files not routed through copy_file"; exit 1; }; grep -q 'for f in kwalletd dolphin gwenview okular kxmlgui5 konsole kate;' "$F" || { echo "FAIL: .local/share loop not extended as specified"; exit 1; }; for t in klipper kdeconnect; do n=$(grep -v '^[[:space:]]*#' "$F" | grep -c -- "$t" || true); [ "$n" -eq 0 ] || { echo "FAIL: $t must be documented as skipped, not copied"; exit 1; }; done; e=$(grep -c -- '--exclude' "$F" || true); [ "$e" -eq 31 ] || { echo "FAIL: exclude line count changed to $e (expected 31: 34 pre-plan minus the 3 lines in the block Task 1 removed)"; exit 1; }; n=$(grep -v '^[[:space:]]*#' "$F" | grep -c '\becho\b' || true); [ "$n" -eq 0 ] || { echo "FAIL: $n bare echo call(s)"; exit 1; }; echo PASS</automated>
  </verify>
  <done>All 18 curated `~/.config` filenames appear in the script and are routed through `copy_file` with the `.config/$f` -> `dotfiles/.config/$f` mapping; the `.local/share` loop reads `kwalletd dolphin gwenview okular kxmlgui5 konsole kate`; the clipboard-history and phone-pairing directories appear only inside comments explaining why they are skipped, never in a copy call; the `--exclude` line total is 31, exactly the pre-plan 34 minus the 3 lines Task 1 removed, proving no user-data block was disturbed; the script parses and still uses `print -r --` throughout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Confirm install-essentials.sh accuracy; record the Claude Code install-channel drift</name>
  <files>dev/system/install-essentials.sh</files>
  <read_first>
    /home/enzief/work/iswi/script/dev/system/install-essentials.sh — the full file.
  </read_first>
  <action>
This task is mostly a confirmation pass. The ground truth is already established: every apt package in this script's lists is installed on this machine except signal-desktop and telegram-desktop, which stay per D-06. oh-my-zsh and all three custom plugins are present. nvm/node v24.18.1, uv, Brave, Firefox, and MEGAsync all match. So the package lists, URLs, repo definitions, and version pins get **no changes at all** — do not add packages, do not remove packages, do not reorder lists, do not "modernize" the `curl | sh` install patterns or the Signal `xenial` suite line. Confirm, do not invent.

There is exactly one real discrepancy worth capturing. The script installs the Claude Code CLI with `npm install -g @anthropic-ai/claude-code`, but on this machine that is not how it is installed: global npm holds only `npm` and `corepack`, while Claude Code lives under `~/.local/share/claude/versions/` (three versions present), which is the native installer's layout.

Do **not** change the install command. Rewriting it would mean substituting an install URL that has not been verified in this session, and the npm channel still produces a working CLI on a fresh machine — it is a channel difference, not a bug. Instead add a two-or-three-line comment directly above that line recording the observation: that as of 260808 this machine has Claude Code via the native installer under `~/.local/share/claude/versions/` rather than from global npm, that the npm line is retained deliberately as the reinstall path because it is verified to work unattended, and that switching channels is an open question rather than an oversight. This is the honest record; the human checkpoint decides whether to act on it.

While reading, confirm and report (do not edit) that: the `NVM_DIR` export still points at `$HOME/.nvm`, which is what Task 1's corrected backup entry now feeds; both `signal-desktop` and `telegram-desktop` are still on the apt line; and the argument-rejection guard, dependency warnings, and `set -uo pipefail` header from the prior task are intact.

Also confirm, without editing either file, that `restore-from-wdhdd.zsh` contains no reference to the old nvm path and no VS Code reference (so D-08 and D-09 have no restore-side mirror), and that `REINSTALL_INSTRUCTIONS.txt` names no filename or package list that this plan changed. Both files must end this plan byte-identical to how they started. If either assumption turns out false, stop and raise it at the checkpoint rather than editing.
  </action>
  <verify>
    <automated>D=/home/enzief/work/iswi/script/dev/system; bash -n "$D/install-essentials.sh" || exit 1; grep -q 'signal-desktop telegram-desktop' "$D/install-essentials.sh" || { echo "FAIL: D-06 packages removed"; exit 1; }; grep -q 'NVM_DIR="\$HOME/.nvm"' "$D/install-essentials.sh" || { echo "FAIL: NVM_DIR changed"; exit 1; }; a=$(grep -c 'apt install' "$D/install-essentials.sh" || true); [ "$a" -eq 8 ] || { echo "FAIL: apt install line count changed to $a (expected 8)"; exit 1; }; grep -q 'local/share/claude' "$D/install-essentials.sh" || { echo "FAIL: install-channel note not recorded"; exit 1; }; grep -q 'npm install -g @anthropic-ai/claude-code' "$D/install-essentials.sh" || { echo "FAIL: install command must not change"; exit 1; }; cd /home/enzief/work/iswi/script && git diff --quiet -- dev/system/restore-from-wdhdd.zsh dev/system/REINSTALL_INSTRUCTIONS.txt || { echo "FAIL: read-only companion files were modified"; exit 1; }; echo PASS</automated>
  </verify>
  <done>`install-essentials.sh` parses under `bash -n`; its 8 `apt install` lines and all package names are unchanged, signal-desktop and telegram-desktop included; `NVM_DIR` still resolves to `$HOME/.nvm`; the npm install command is intact and now carries a comment recording the observed native-installer drift; `restore-from-wdhdd.zsh` and `REINSTALL_INSTRUCTIONS.txt` show no git diff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human review — read the diff, especially the curated list</name>
  <what-built>
    `backup-to-wdhdd.zsh` now reflects the post-migration Kubuntu machine: the nvm Tier-2 prompt points at `~/.nvm` (it had always named a path nvm never creates, so it copied nothing for its entire life) with the size corrected from 1.1G to a measured 236M; the VS Code Tier-2 block is gone (not installed here); and Tier 1 gained 18 curated loose KDE rc files under `~/.config` plus `konsole` and `kate` under `.local/share`, with a comment block recording what was deliberately skipped and why. `install-essentials.sh` gained one comment noting that Claude Code is actually installed here via the native installer rather than global npm; its package lists are untouched. `restore-from-wdhdd.zsh` and `REINSTALL_INSTRUCTIONS.txt` were deliberately not changed.
  </what-built>
  <how-to-verify>
    These scripts cannot be run without wiping a machine, so reading the diff is the verification.

      cd /home/enzief/work/iswi/script
      git diff -- dev/system/

    Reading it, confirm:

      1. Nothing was lost. Every exFAT note, every 260731-dated decision, the symlink runaway-copy story, the obsolete-subtree paragraph, the `round/nubank` warning, and every Tier-2 size annotation are still there. The only deletion should be the VS Code block.
      2. No user-data section moved. The `work/`, `Documents/`, `Pictures/`, `Apps/`, `Downloads/`, `Videos/`, `renikov/`, `_recover/`, and loose-files blocks should not appear in the diff at all.
      3. The curated list is the right list. This is the judgment call worth your eyes — scan the 18 filenames and say whether anything obviously valuable is missing or anything there is noise.

    Then a live sanity check that every curated name actually exists on this machine (expect no output):

      for f in kdeglobals kglobalshortcutsrc kwinrc kwinoutputconfig.json plasma-org.kde.plasma.desktop-appletsrc plasmashellrc kxkbrc mimeapps.list dolphinrc konsolerc konsolesshconfig katerc kate-externaltoolspluginrc katevirc gwenviewrc okularrc okularpartrc spectaclerc; do [ -f "$HOME/.config/$f" ] || echo "ABSENT: $f"; done

    Three specific decisions to accept or overturn:

      a. **kdeconnect was skipped**, despite being genuine config, because it holds an unencrypted device private key and the restore script's permission-fix pass only covers `.ssh`/`.gnupg`/`.aws` — it would be restored world-readable. Re-pairing a phone takes seconds. Agree, or should it be added (and if so, should the restore script's permission pass be extended to cover it)?
      b. **Claude Code's install channel drifted.** The script installs it via `npm install -g`, but this machine has it under `~/.local/share/claude/versions/` from the native installer. The plan recorded this as a comment rather than rewriting the install command, because the replacement URL was not verified in this session. Leave as-is, or switch the script to the native installer?
      c. **Power, session, lock, and input rc files were excluded** (`powerdevilrc`, `powermanagementprofilesrc`, `ksmserverrc`, `kscreenlockerrc`, `kcminputrc` — together under 1KB) as near-default plumbing rather than deliberate choices. If any of those hold a setting you actually tuned, say so and they can be added.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| home directory -> external exFAT drive | `backup-to-wdhdd.zsh` writes selected config onto a removable volume that stores no Unix permissions and is not encrypted |
| external drive -> fresh machine | `restore-from-wdhdd.zsh` copies everything under `dotfiles/` back into `$HOME` with permissions defaulted by umask |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-07 | Information Disclosure | `.config/kdeconnect` private key on exFAT | high | mitigate | Deliberately excluded from the curated list (Task 2), with the reasoning recorded in a comment so a future reader does not "helpfully" add it. The restore script's permission-fix pass covers only `.ssh`/`.gnupg`/`.aws`, so the key would land world-readable if copied. |
| T-Q-08 | Information Disclosure | `.local/share/klipper` clipboard-history sqlite | medium | mitigate | Deliberately excluded (Task 2). Clipboard history routinely contains pasted passwords and tokens; it is user data with no configuration value, and the exFAT drive is unencrypted. |
| T-Q-09 | Information Disclosure | The 18 newly curated rc files themselves | low | accept | Verified to hold theme, shortcut, layout, and per-app-preference data only — no credentials, tokens, or keys. Largest is `kglobalshortcutsrc` at 15K of key bindings. |
| T-Q-10 | Tampering | The dead nvm Tier-2 entry (pre-fix) | low | mitigate | Task 1 (D-08). A prompt that always answered into a no-op trains the operator to trust prompts that do nothing; repointing it at the real path makes the answer meaningful. |
| T-Q-SC | Tampering | npm/pip/cargo installs | n/a | n/a | This plan installs no packages. The `npm install -g @anthropic-ai/claude-code` line in `install-essentials.sh` is pre-existing content that this plan explicitly does not modify or execute. No legitimacy audit required. |
</threat_model>

<verification>
Run from `/home/enzief/work/iswi/script`:

1. Both edited scripts parse with their declared interpreters:
   `zsh -n dev/system/backup-to-wdhdd.zsh && bash -n dev/system/install-essentials.sh`
2. Only the two intended files changed — expect exactly `dev/system/backup-to-wdhdd.zsh` and `dev/system/install-essentials.sh`:
   `git diff --name-only -- dev/system/`
3. The zsh script still uses the repo's output convention — expect `0`:
   `grep -hv '^[[:space:]]*#' dev/system/backup-to-wdhdd.zsh | grep -c '\becho\b'`
4. Exactly 8 Tier-2 confirm blocks remain, one fewer than before:
   `grep -c 'if confirm ' dev/system/backup-to-wdhdd.zsh`
5. Every curated `~/.config` filename resolves on this machine — expect no output:
   `for f in kdeglobals kglobalshortcutsrc kwinrc kwinoutputconfig.json plasma-org.kde.plasma.desktop-appletsrc plasmashellrc kxkbrc mimeapps.list dolphinrc konsolerc konsolesshconfig katerc kate-externaltoolspluginrc katevirc gwenviewrc okularrc okularpartrc spectaclerc; do [ -f "$HOME/.config/$f" ] || echo "ABSENT: $f"; done`
6. The external drive was never touched (D-04) — expect no output from a plan that never reads or writes it:
   `git diff -- dev/system/ | grep -c 'media/enzief/wdhdd/lubuntu'` should equal the count of pre-existing hardcoded `DEST` references only (0 new lines added or removed containing that path).
</verification>

<success_criteria>
- `backup-to-wdhdd.zsh` and `install-essentials.sh` are the only modified files
- The nvm Tier-2 entry copies `.nvm/` to `dotfiles/.nvm/`, keeps its `[recommend: No]` wording, and carries a corrected size annotation (D-08)
- The VS Code Tier-2 confirm block is gone, leaving 8 confirm blocks (D-09)
- All 18 curated `~/.config` loose files are backed up via `copy_file`, and `konsole` and `kate` were appended to the `.local/share` loop (D-07)
- The clipboard-history and phone-pairing directories appear only in skip-explaining comments, never in a copy call
- `install-essentials.sh`'s apt package lists are unchanged, signal-desktop and telegram-desktop included (D-06)
- No user-data backup section was modified; the `--exclude` line count is 31 (34 pre-plan, minus the 3 lines inside the removed block)
- Every pre-existing tribal-knowledge comment survives; the VS Code block is the only deletion (D-05)
- `restore-from-wdhdd.zsh` and `REINSTALL_INSTRUCTIONS.txt` show no git diff
- Nothing under `/media/enzief/wdhdd/lubuntu/` was read or written (D-04)
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/quick/260808-8v1-update-dev-system-install-essentials-sh-/260808-8v1-SUMMARY.md` when done.
</output>
