---
status: complete
phase: quick-260808-8v1
plan: 01
subsystem: dev/system
tags: [backup, kde-plasma, kubuntu, reinstall-scripts]
dependency-graph:
  requires: [260808-8a3]
  provides: []
  affects: [dev/system/backup-to-wdhdd.zsh, dev/system/install-essentials.sh]
tech-stack:
  added: []
  patterns:
    - "copy_file() for loose regular files under ~/.config (silent skip on missing source)"
    - "copy() for directories (warn-and-skip on missing source)"
key-files:
  created: []
  modified:
    - dev/system/backup-to-wdhdd.zsh
    - dev/system/install-essentials.sh
decisions:
  - "D-08: repointed dead .config/nvm Tier-2 entry to the real .nvm path; corrected size 1.1G -> 236M"
  - "D-09: deleted the VS Code Tier-2 confirm block outright (not installed on this machine)"
  - "D-07: added 23 curated ~/.config loose rc files to Tier 1 via copy_file (18 original + 5 power/session/lock/input files added at checkpoint), plus konsole/kate to the .local/share loop"
  - "install-essentials.sh package lists confirmed unchanged (D-06); Claude Code install switched from npm to Anthropic's native installer at checkpoint, matching this machine's actual install method"
  - "Checkpoint (Task 4): kdeconnect skip confirmed as-is; Claude Code switched to native installer (curl -fsSL https://claude.ai/install.sh | bash); powerdevilrc/powermanagementprofilesrc/ksmserverrc/kscreenlockerrc/kcminputrc added to curated Tier 1 (user confirmed tuned settings in these files)"
metrics:
  duration: ~35min
  completed: 2026-08-08
actuals:
  tokens: 10800
  tasks: 4
  commits: 5
---

# Phase quick-260808-8v1 Plan 01: Update dev/system backup and install-essentials scripts Summary

Repointed a dead Tier-2 nvm backup path, removed a Tier-2 entry for uninstalled software, and added curated KDE Plasma desktop-config coverage to `backup-to-wdhdd.zsh`'s Tier 1 so a fresh-machine restore recovers panel layout, global shortcuts, window rules, and per-app settings without manual reconstruction. Confirmed `install-essentials.sh` needs no package-list changes; at the human checkpoint, switched its Claude Code install to Anthropic's native installer (matching this machine's actual install method) and extended the curated rc-file list with five power/session/lock/input files the user confirmed hold tuned settings.

**Status: Complete. All 4 tasks done, checkpoint approved with three resolved follow-ups.**

## Tasks Completed

### Task 1: Correct the two stale Tier-2 entries (D-08, D-09)

- Commit `79fe4c2`
- The `.config/nvm` Tier-2 prompt named a path nvm never creates (nvm installs to `$HOME/.nvm`), so its existence guard silently caught nothing for the script's entire life. Repointed the prompt text and `copy` call to `.nvm/` -> `dotfiles/.nvm/`, kept the `[recommend: No]` wording, corrected the size annotation from a claimed 1.1G to the measured 236M, and added a tribal-knowledge comment recording why.
- Deleted the VS Code Tier-2 confirm block entirely (prompt, `copy` call, and its 5 excludes) — VS Code is not installed on this machine (no config dir, no binary on PATH). This is the plan's one sanctioned deletion.
- Verified: `zsh -n` parses clean; exactly 8 Tier-2 `confirm` blocks remain (was 9); no live reference to the old nvm path or the VS Code block survives; every pre-existing tribal-knowledge marker (`260731`, `exFAT`, `symlinks`, `obsolete`, `nubank`, `hakuneko`, `Crashpad`) is intact.

### Task 2: Add curated KDE Plasma config coverage to Tier 1 (D-07)

- Commit `37e5bf6`
- Added a new `copy_file`-driven loop backing up 18 curated loose files directly under `~/.config`: Plasma/global (`kdeglobals`, `kglobalshortcutsrc`, `kwinrc`, `kwinoutputconfig.json`, `plasma-org.kde.plasma.desktop-appletsrc`, `plasmashellrc`, `kxkbrc`), associations (`mimeapps.list`), and per-app (`dolphinrc`, `konsolerc`, `konsolesshconfig`, `katerc`, `kate-externaltoolspluginrc`, `katevirc`, `gwenviewrc`, `okularrc`, `okularpartrc`, `spectaclerc`) — ~35KB total.
- Extended the existing `.local/share` loop with `konsole` and `kate` (saved terminal profiles, editor session state), keeping the five prior entries first and unchanged.
- Added a comment block documenting deliberate skips: `.local/share/klipper` (clipboard-history secrets on an unencrypted drive), `.config/kdeconnect` (unencrypted device private key, restore script's permission-fix pass doesn't cover it), and `.local/share/claude` (redownloadable release binaries — `$HOME/.claude`, the real config, is already backed up elsewhere).
- Verified: all 18 filenames present and routed through `copy_file` with the exact `.config/$f` -> `dotfiles/.config/$f` mapping; `.local/share` loop reads `kwalletd dolphin gwenview okular kxmlgui5 konsole kate`; `klipper`/`kdeconnect` appear only in comments, never in a copy call; `--exclude` line count is 31 (34 pre-plan minus the 3 removed with the VS Code block), proving no user-data section was disturbed; script still parses and uses `print -r --` throughout (zero bare `echo` calls).

### Task 3: Confirm install-essentials.sh accuracy; record the Claude Code install-channel drift

- Commit `f8e1aac`
- Confirmed (no edits) that every apt package, URL, repo definition, and version pin in `install-essentials.sh` matches this machine's actual state — signal-desktop and telegram-desktop stay per D-06 even though neither is installed here.
- Added a 4-line comment directly above the `npm install -g @anthropic-ai/claude-code` line recording that this machine actually has Claude Code via the native installer under `~/.local/share/claude/versions/`, not global npm. The install command itself is unchanged — deliberately, since the npm channel is verified to work unattended and switching install channels is an open question for the human checkpoint, not an oversight.
- Confirmed and left untouched: `NVM_DIR` still points at `$HOME/.nvm`; both `signal-desktop` and `telegram-desktop` remain on the apt line; the argument-rejection guard, dependency warnings, and `set -uo pipefail` header are intact.
- Confirmed `restore-from-wdhdd.zsh` has no nvm or VS Code reference (so D-08/D-09 have no restore-side mirror to update) and `REINSTALL_INSTRUCTIONS.txt` names no filename or package list this plan changed. Both files verified byte-identical via `git diff` — no edits made to either.

## Deviations from Plan

None — plan executed exactly as written for Tasks 1-3.

## Verification Results

All automated `<verify>` blocks for Tasks 1-3 passed. The plan's overall `<verification>` section was also run manually and passed, scoped correctly to this task's own commit range (`a100d2d..HEAD`, excluding the prior unrelated 260808-8a3 adoption commits that share the same `dev/system/` directory):

1. Both scripts parse (`zsh -n`, `bash -n`) — OK
2. Only `dev/system/backup-to-wdhdd.zsh` and `dev/system/install-essentials.sh` changed in this task's commits — confirmed
3. Zero bare `echo` calls (outside comments) in `backup-to-wdhdd.zsh` — confirmed
4. Exactly 8 Tier-2 `confirm` blocks remain — confirmed
5. All 18 curated `~/.config` filenames resolve on this machine — confirmed, no `ABSENT` output
6. No new references to `/media/enzief/wdhdd/lubuntu/` were introduced (D-04) — confirmed, 0 new occurrences

### Task 4: Human review — checkpoint resolved

The user reviewed the diff, ran the live sanity check (all 18 curated filenames confirmed present, no `ABSENT` output), and answered the three open questions:

- **(a) kdeconnect** — confirmed SKIP. No change; the Task 2 decision stands as originally committed.
- **(b) Claude Code install channel** — SWITCH to the native installer. `install-essentials.sh` now runs `curl -fsSL https://claude.ai/install.sh | bash` (Anthropic's documented native install method) instead of `npm install -g @anthropic-ai/claude-code`. This matches the machine's actual install method (`~/.local/share/claude/versions/`) and the existing `uv` install pattern in the same section. The prior drift-tracking comment (added in commit `f8e1aac`) was removed since it's no longer describing a drift. Commit `a8a86bb`.
- **(c) Power/session/lock/input rc files** — ADD all five. The user confirmed tuned settings exist in one or more of `powerdevilrc`, `powermanagementprofilesrc`, `ksmserverrc`, `kscreenlockerrc`, `kcminputrc`. Added to the curated Tier-1 loop in `backup-to-wdhdd.zsh`, bringing the curated file count from 18 to 23 (still under 1KB added, so Tier 1 without a confirm prompt remains correct). The loop's size comment was corrected from `~35KB` to `~38KB` total. Commit `ae9a568`.

Post-checkpoint verification re-run and passed: `zsh -n`/`bash -n` syntax checks clean; Tier-2 confirm-block count still 8 (unaffected, these were Tier-1 additions); `--exclude` count still 31 (unaffected); new `curl -fsSL https://claude.ai/install.sh | bash` line present and old `npm install -g @anthropic-ai/claude-code` line removed; `restore-from-wdhdd.zsh` and `REINSTALL_INSTRUCTIONS.txt` confirmed still untouched via `git diff`.

## Self-Check: PASSED

- `dev/system/backup-to-wdhdd.zsh` — FOUND
- `dev/system/install-essentials.sh` — FOUND
- Commit `79fe4c2` — FOUND in `git log --oneline --all`
- Commit `37e5bf6` — FOUND in `git log --oneline --all`
- Commit `f8e1aac` — FOUND in `git log --oneline --all`
- Commit `a8a86bb` — FOUND in `git log --oneline --all`
- Commit `ae9a568` — FOUND in `git log --oneline --all`
