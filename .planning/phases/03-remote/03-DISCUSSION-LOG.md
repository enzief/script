# Phase 3: Remote - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-07
**Phase:** 3-Remote
**Areas discussed:** Matching bug diagnosis, Size-collision handling, Dependency checks, Shadow-file validation behavior, Shadow-file sourcing safety, Remote config storage

---

**Process note:** the interactive AskUserQuestion prompt for area selection was rejected by the user, who asked Claude to "pick default verdict and go." No per-area AskUserQuestion turns were run. The table below reflects the options Claude considered and the default it picked for each, not a user selection from a presented menu.

---

## Matching bug diagnosis (REMOTE-01)

| Option | Description | Selected |
|--------|-------------|----------|
| Treat as confirmed bug, fix unconditionally | Assume `remote_map` is genuinely unreadable in the `find \| while` loop and rewrite the loop regardless | |
| Verify empirically first, frame as hardening if disproven | Re-run the same kind of check that disproved LOCALFS-01/02 in Phase 1 before deciding fix-vs-hardening framing | ✓ |

**Claude's choice:** Verify first. A quick zsh test during this discussion showed the array is already readable/writable across the `find \| while` loop (last pipeline element runs in the current shell in zsh) — same pattern as Phase 1's misdiagnosis.
**Notes:** Left for research/planning to confirm formally against the real script; CONTEXT.md D-01 documents the test and the fallback framing either way.

---

## Size-collision handling (adjacent to REMOTE-01)

| Option | Description | Selected |
|--------|-------------|----------|
| Add ambiguity warning, keep first-match behavior | Visibility-only change, no control-flow change (mirrors Phase 2's D-01/D-02 pattern) | |
| Leave as-is, no new behavior | Matching stays first-write-wins with no warning; collision risk already acknowledged in the script's own comment | ✓ |

**Claude's choice:** Leave as-is — not part of REMOTE-01's literal scope, and the original author already accepted this trade-off explicitly in-code.
**Notes:** Not deferred as a future idea since it's a already-made, known call, not an unaddressed gap.

---

## Dependency checks (REMOTE-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Add rclone/jq checks to both scripts per requirement's literal wording | Matches REMOTE-01 text exactly | |
| Add checks only to the script that actually calls each tool | Script 1 uses rclone+jq; script 2 uses neither | ✓ |

**Claude's choice:** Checks only where used — script 2's body never calls `rclone` or `jq`.
**Notes:** Flagged as an interpretation of the requirement, in case the user intended something else by mentioning both scripts.

---

## Shadow-file validation behavior (REMOTE-03)

| Option | Description | Selected |
|--------|-------------|----------|
| Skip file, warn, continue run | Matches existing pattern at rename-remote-files-2-rename-local.zsh:39-40 | ✓ |
| Abort entire run on first invalid shadow file | Stricter, but stops mid-batch | |

**Claude's choice:** Skip-and-continue, consistent with the script's existing warn-and-continue convention one branch down.

---

## Shadow-file sourcing safety (adjacent to REMOTE-03)

| Option | Description | Selected |
|--------|-------------|----------|
| Switch from `source` to grep/awk parsing | Closes the arbitrary-code-execution risk CONCERNS.md flags | |
| Keep `source`, add only the presence/non-empty check | Minimal, matches REMOTE-03's literal wording | ✓ |

**Claude's choice:** Keep `source` + validate. Shadow files are self-generated by script 1 in the same trusted single-user workflow; switching parsers is unrequested scope growth.

---

## Remote config storage (REMOTE-04)

| Option | Description | Selected |
|--------|-------------|----------|
| Environment variables, required, fail fast if unset | Simplest, no new file format or gitignore entry | ✓ |
| Git-ignored config file sourced by the script | More setup (new file, gitignore entry) for a single-user tool | |

**Claude's choice:** Environment variables — matches "no new languages/runtimes," pure-shell, lightweight-process constraints.

---

## Claude's Discretion

- Exact env var names (defaulted to keeping `REMOTE_NAME`/`REMOTE_PATH` unprefixed)
- Exact fix mechanism if REMOTE-01's bug is confirmed real (defaulted to process substitution, matching Phase 1 precedent)
- Test fixture/scope details for the required tracked test file

## Deferred Ideas

None new. REMOTE-05 (dry-run) and REMOTE-06 (configurable rate limiting) remain deferred to v2 per `REQUIREMENTS.md` (pre-existing, not re-raised this session).
